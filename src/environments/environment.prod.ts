import { EnvConfig } from './environment-config';

export const environment: EnvConfig = {
  hostName: 'localhost',
  production: true,
  apis: {
    hackerNews: 'https://hackernewsapi.azure-api.net',
  },
};
